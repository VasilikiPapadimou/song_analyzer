# 🎵 Song Analyzer

> An AI-powered lyrics analysis pipeline that transforms raw song lyrics into structured, validated JSON using Large Language Models.

![Python](https://img.shields.io/badge/Python-3.13-blue)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o--mini-green)
![uv](https://img.shields.io/badge/Package%20Manager-uv-purple)
![License](https://img.shields.io/badge/Status-In%20Development-orange)

---

# Overview

Song Analyzer is an end-to-end AI pipeline that transforms raw song lyrics into a structured JSON representation.

Rather than asking an LLM to "analyze a song" and accepting free-form text, this project treats the language model as **one component of a larger AI system**.

The application:

- validates the input
- preprocesses lyrics
- constructs a structured prompt
- sends the request to an LLM
- parses the returned JSON
- validates the response against a JSON Schema
- enriches it with deterministic metadata
- stores reproducible outputs

The goal is not simply to generate song analyses, but to demonstrate how Large Language Models can be integrated into reliable software systems.

---
## Key Highlights

- 🤖 AI-powered lyrics analysis using GPT-4o-mini
- 🧩 Structured JSON outputs with schema validation
- 🛡️ Multi-stage validation pipeline for reliable AI responses
- 📂 Reproducible folder structure and metadata tracking
- 🏗️ Modular architecture separating deterministic code from LLM reasoning

---
# Project Motivation

Large Language Models are incredibly powerful, but integrating them into production software requires much more than calling an API.

This project explores questions such as:

- How can we reliably process unstructured text?
- How do we validate AI-generated outputs?
- How do we separate deterministic logic from probabilistic reasoning?
- How do we design AI pipelines that are reproducible and maintainable?

The result is an end-to-end AI pipeline that emphasizes reliability, reproducibility, and structured processing rather than treating the LLM as a black-box text generator.
---

# Features

## Current Features

- ✅ Manual song import with defined input structure
- ✅ Input parsing and validation
- ✅ Lyrics preprocessing and metadata removal
- ✅ Sequential lyric-line numbering for evidence grounding
- ✅ Versioned prompt construction
- ✅ OpenAI API integration
- ✅ Strict structured JSON output
- ✅ JSON Schema validation for LLM responses
- ✅ Deterministic metadata generation
- ✅ Final `analysis.json` schema validation
- ✅ Deterministic evidence validation
- ✅ Emotional-arc positional validation and warnings
- ✅ Automatic ISO-week output folder creation
- ✅ Persistent application logging
- ✅ Unit tests for preprocessing and deterministic validation
- ✅ Package-based modular architecture

## Planned Features

- ⏳ Retry and failure-handling policy
- ⏳ Failed LLM response preservation
- ⏳ Token usage and API cost tracking
- ⏳ Duplicate song detection
- ⏳ Semantic analysis-quality evaluation
- ⏳ Human-reviewed evaluation dataset
- ⏳ Quality metrics and acceptance criteria
- ⏳ Weekly aggregation of accepted song analyses
- ⏳ Automated Markdown and PDF reports
- ⏳ Multi-model comparison
- ⏳ Agentic workflow extensions

---
# System Design Decisions

This project was intentionally designed as an AI system rather than a simple LLM wrapper.

The objective was to separate deterministic software engineering responsibilities from probabilistic AI reasoning, making the pipeline more reliable, reproducible, and maintainable.

Key design decisions include:

- **Separation of responsibilities**
  - Python handles orchestration, validation, file management, metadata generation, and persistence.
  - The LLM is responsible only for semantic analysis.

- **Structured outputs instead of free text**
  - The LLM is instructed to return structured JSON instead of natural language paragraphs.
  - This allows downstream processing without additional parsing.

- **Schema-first validation**
  - Every AI response is validated against a predefined JSON Schema before it is accepted.
  - Invalid outputs are rejected instead of silently propagating through the pipeline.

- **Deterministic metadata**
  - Information such as processing timestamps, prompt version, model version, and song metadata is generated by Python rather than inferred by the LLM.

- **Reproducible outputs**
  - Every analysis is stored using a deterministic folder structure to make experiments repeatable and easy to inspect.

- **Modular architecture**
  - Input processing, prompt generation, LLM communication, parsing, validation, logging, and file management are implemented as separate modules to improve maintainability and testability.

---
# Architecture

```
               ┌─────────────────┐
               │  Song Text File │
               └────────┬────────┘
                        │
                        ▼
                  Input Parsing
                        │
                        ▼
              Lyrics Preprocessing
                        │
                        ▼
                 Line Numbering
                        │
                        ▼
                Prompt Construction
                        │
                        ▼
                   OpenAI API
                        │
                        ▼
                   JSON Parsing
                        │
                        ▼
           SONG_ANALYSIS_SCHEMA Validation
                        │
                        ▼
                Metadata Construction
                        │
                        ▼
          FINAL_ANALYSIS_SCHEMA Validation
                        │
                        ▼
              Deterministic Validation
                        │
                        ▼
                  analysis.json
```

*A visual workflow diagram will be added in a future update.*

---
# Installation

Clone the repository:

```bash
git clone https://github.com/VasilikiPapadimou/song_analyzer.git

cd song_analyzer
```

Install the project dependencies:

```bash
uv sync
```

Create a `.env` file in the project root:

```text
OPENAI_API_KEY=your_api_key_here
```

Run the application:

```bash
uv run python -m song_analyzer
```

---

# Example Input

Each input file follows this structure:

```text
- Line 1: artist
- Line 2: song title
- Line 3 onward: lyrics
```

---

# Example Output

The pipeline generates a validated JSON file containing:

- song metadata
- structured lyrics analysis
- uncertainty assessment
- processing metadata

Example:

```json
{
  "metadata": {
    "model": "gpt-4o-mini",
    "processed_at": "2026-07-31T10:15:00"
  },
  "lyrics_analysis": {
    ...
  },
  "uncertainty": {
    ...
  }
}
```

---

# Validation Pipeline

One of the primary goals of this project is demonstrating that LLM outputs should **never be blindly trusted**.

Every AI response passes through multiple validation stages:

```
LLM Response
      │
      ▼
JSON Parsing
      │
      ▼
Structural Validation
SONG_ANALYSIS_SCHEMA
      │
      ▼
Metadata Assembly
      │
      ▼
Structural Validation
FINAL_ANALYSIS_SCHEMA
      │
      ▼
Deterministic Validation
      │
      ├── Evidence line validity
      ├── Empty evidence checks
      ├── Duplicate evidence warnings
      ├── Starting/ending position warnings
      └── Turning-point chronology
      │
      ▼
Accepted analysis.json
```

Structural validation verifies that the LLM output follows the expected JSON contract. Deterministic validation then checks facts that Python can verify directly, such as evidence-line validity and positional consistency. Semantic quality evaluation is planned as a separate later stage.

---

# Tech Stack

### AI

- OpenAI GPT-4o-mini
- Prompt Engineering

### Python

- Python 3.13
- uv
- python-dotenv
- jsonschema

### Development

- VS Code
- Git
- GitHub

---

# Skills Demonstrated

## AI Engineering

- Prompt Engineering
- LLM Integration
- Structured Outputs
- JSON Schema Validation
- AI Pipeline Design

## Software Engineering

- Python
- Logging
- Error Handling
- File Management
- Project Architecture
- Dependency Management
- Environment Configuration
- Git

---

# Project Structure

```text
song_analyzer/
│
├── data/
│   ├── imports/
│   └── exports/
│
├── logs/
│
├── src/
│   └── song_analyzer/
│       ├── __init__.py
│       ├── __main__.py
│       ├── config.py
│       ├── file_handling.py
│       ├── input_preprocess.py
│       ├── pipeline.py
│       │
│       ├── llm/
│       │   ├── __init__.py
│       │   ├── client.py
│       │   └── prompts.py
│       │
│       ├── schemas/
│       │   ├── __init__.py
│       │   ├── analysis.py
│       │   └── taxonomies.py
│       │
│       └── validation/
│           ├── __init__.py
│           ├── deterministic.py
│           └── structural.py
│
├── tests/
│   ├── test_analysis_validator.py
│   └── test_input_preprocess.py
│
├── pyproject.toml
├── uv.lock
├── README.md
└── .gitignore
```

---
# Roadmap

### Reliability Layer

- [ ] Retry and failure-handling architecture
- [ ] Preserve failed LLM attempts for inspection
- [ ] Token usage and cost tracking
- [ ] Duplicate song detection

### Evaluation Layer

- [ ] Semantic analysis evaluator
- [ ] Human-reviewed evaluation dataset
- [ ] Evidence-grounding metrics
- [ ] Theme and emotion agreement metrics
- [ ] Emotional-arc, agency, and resolution evaluation
- [ ] Analysis acceptance criteria

### Weekly Analysis

- [ ] Aggregate accepted analyses by ISO week
- [ ] Generate weekly Markdown report
- [ ] Generate PDF report
- [ ] Dashboard for historical analyses

### Future Extensions

- [ ] Multi-model comparison
- [ ] Agentic workflow

---

# Lessons Learned

Building reliable AI systems is less about calling an LLM and more about designing deterministic software around probabilistic components.

This project focuses on making AI outputs reproducible, validated, explainable, and maintainable.

---

## Author

**Vasiliki Papadimou**

AI Systems Engineering Portfolio Project

GitHub: https://github.com/VasilikiPapadimou

LinkedIn: *(Coming Soon)*